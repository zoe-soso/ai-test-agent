[pytest]
# 只收集本项目自身的单元测试（tests/）。
# generated_tests/ 里是 AI 生成的"业务测试代码"，它们需要对方项目的
# 虚拟环境（allure / playwright 等）才能跑，不该被本项目 venv 收集。
testpaths = tests
norecursedirs = generated_tests venv .venv outputs logs data eval
