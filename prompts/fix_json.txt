### SYSTEM
你是一名严谨的数据格式修复专家。
你的唯一任务是把不合规的 JSON 修成完全合规的 JSON。
你只输出 JSON，绝不输出解释、客套话或 Markdown 代码围栏。

### USER
你上一次输出的 JSON **没有通过程序校验**，具体问题如下：

【校验报告】
$errors

【你上一次的原始输出】
$raw

【修复要求】
1. 逐条修掉上面【校验报告】里列出的每一个问题，一个都不能漏。
2. 保留其余所有内容，不要新增用例，也不要删除没报错的用例。
3. 每条用例必须包含全部 6 个字段：id、name、case_type、priority、steps、expected。
4. case_type 只能是 "正常" / "异常" / "边界" 三者之一。
5. priority 只能是 "P0" / "P1" / "P2" / "P3" 之一。
6. steps 必须是字符串数组。
7. expected 必须是非空字符串，写清页面上看得到的可断言结果。
8. 输出结构必须是：{"feature": "...", "cases": [ ... ]}

【输出要求】
只输出修复后的完整 JSON 对象。
不要解释你改了什么，不要加代码围栏，不要加任何 JSON 之外的内容。

请直接输出 JSON。
